package com.example.amethystore.worldgen;

import com.example.amethystore.AmethystOreMod;
import net.minecraftforge.eventbus.api.IEventBus;
import net.minecraftforge.fml.common.Mod;

/**
 * This class is kept as a placeholder.
 * World generation is now handled through JSON files in data/amethystore/worldgen/
 */
@Mod.EventBusSubscriber(bus = Mod.EventBusSubscriber.Bus.MOD)
public class ModConfiguredFeatures {
    
    public static void register(IEventBus eventBus) {
        // Registration now handled via JSON files
    }
}
